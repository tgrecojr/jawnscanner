Your security hook blocked .env.example (matched .env.* pattern). You can create it manually — it just contains:
  LISTEN_PORT=9101
  PHL_API_URL=https://www.phl.org/phllivereach/metrics
  RUST_LOG=info